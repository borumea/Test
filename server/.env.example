# Database Configuration
# The server prioritizes .env values over db.config.legacy.js (if it exists)

# Database connection settings
DB_HOST=localhost
DB_USER=root
DB_PASS=your_password_here
DB_NAME=your_database_name

# Server port
PORT=3001

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_here_change_in_production

# Encryption key for config values (optional)
# If you want to store encrypted passwords in .env, set this key
# and use: node server/Utils/encrypt-config.js encrypt "your_password"
CONFIG_ENCRYPTION_KEY=your_encryption_key_here

# Node environment
NODE_ENV=development

# ============================================
# ENCRYPTION SUPPORT
# ============================================
# You can store values in plain text OR encrypted format.
# The server will automatically detect and decrypt encrypted values.
#
# To encrypt a value:
#   1. Set your CONFIG_ENCRYPTION_KEY above
#   2. Run: CONFIG_ENCRYPTION_KEY=your_key node server/Utils/encrypt-config.js encrypt "your_password"
#   3. Copy the encrypted output to your .env file
#
# Example:
#   DB_PASS=U2FsdGVkX1+XqK5J... (encrypted)
#   DB_PASS=myPlainPassword     (plain text - also works)
#
# To decrypt a value (for verification):
#   CONFIG_ENCRYPTION_KEY=your_key node server/Utils/encrypt-config.js decrypt "U2FsdGVkX1..."
#
# To check if a value is encrypted:
#   node server/Utils/encrypt-config.js check "your_value"
